# Build output
/artigate

# Test/coverage artifacts
coverage.out
coverage.txt

# Local signing keys — never commit these
*.ed25519
*.ed25519.pub

# Local Compose credentials — copy from .env.example and keep private
/.env

# Local Claude preview config (machine-specific paths)
.claude/launch.json
